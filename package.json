{
  "name": "cueramaro-prime",
  "version": "1.0.0",
  "description": "Sistema POS local-first para Cuerámaro",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "docker:up": "docker-compose -f infra/compose/docker-compose.yml up -d",
    "docker:down": "docker-compose -f infra/compose/docker-compose.yml down"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
